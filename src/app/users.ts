export class Users {
      id: number;
      login: string;
      nome: string;
      password: number;

      constructor(id: number, login: string, nome: string, password: number;)
      {
        this.id = id;
        this.login = login;
        this.nome = nome;
        this.password = password;

      }
}
